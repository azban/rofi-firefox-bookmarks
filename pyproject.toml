[project]
name = "rofi-firefox-bookmarks"
version = "0.1.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["rofi_firefox_bookmarks"]

[project.scripts]
rofi-firefox-bookmarks = "rofi_firefox_bookmarks:main"
